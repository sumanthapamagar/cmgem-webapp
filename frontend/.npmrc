# npm configuration for Azure Pipeline builds
cache=C:\npm-cache
prefer-offline=false
fund=false
audit=false
# Force rebuild of native binaries
rebuild-bundle=true
# Ensure proper platform detection
target_platform=win32
target_arch=x64

